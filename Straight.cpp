#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[]) {
  /*int n, k, item, sum = 0;  cin>>n>>k;
  int* arr = new int[n];
  for(int i = 0; i < n; i++)   {  cin>>item;  sum += item;  }

  if(ceil(k - (float)sum/n) > 0)
    cout<<ceil(k - (float)sum/n)<<endl;
  else
   cout<<"0"<<endl;
  */
  return 0;
}
